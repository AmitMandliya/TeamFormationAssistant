language: python
python: 
    - "3.7"

services:
    - docker

before_install:
    - docker-compose up --build -d

script:
    - docker exec ml_server python -m pytest

after_script:
    docker-compose down 
